---
name: afl:delegate
description: 구현 위임 - 확정된 Checkpoint를 자율 에이전트에게 위임하여 자동 구현/검증
argument-hint: "<checkpoint-id> | --session <session-id> [--all]"
allowed-tools: ["Task", "Bash", "Read", "Write", "Glob", "Grep", "AskUserQuestion"]
---

# Delegate Command

확정된 Checkpoint를 **자율 에이전트**에게 위임합니다.

## 핵심 원칙

```
┌─────────────────────────────────────────────────────────────────┐
│  AUTONOMOUS DELEGATION: 에이전트가 "어떻게"를 자율적으로 결정   │
│                                                                 │
│  인간은:                                                        │
│  • 위임 시작만 결정                                             │
│  • 에스컬레이션 시에만 개입                                     │
│                                                                 │
│  에이전트는:                                                    │
│  • 구현 방법 자율 결정                                          │
│  • Checkpoint 기준으로 자동 검증                                │
│  • 실패 시 자동 분석 + 재시도                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 사용법

```bash
# 특정 Checkpoint 구현 위임
/afl:delegate coupon-service

# 세션의 모든 Checkpoint 병렬 위임
/afl:delegate --session abc12345 --all

# 특정 세션의 특정 Checkpoint 위임
/afl:delegate --session abc12345 coupon-api

# 실패한 Checkpoint 재시도
/afl:delegate --retry coupon-service
```

---

## 실행 절차

```
/afl:delegate --session abc12345 --all
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│  STEP 1: 사전 검증                                            │
│                                                               │
│  • 세션 존재 확인                                             │
│  • Checkpoint 승인 여부 확인                                  │
│  • 의존성 그래프 분석                                         │
└───────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│  STEP 2: 실행 계획 수립                                       │
│                                                               │
│  의존성 기반 실행 순서:                                       │
│                                                               │
│  Round 1 (병렬):                                              │
│    ├── coupon-model (의존성 없음)                             │
│    └── ...                                                    │
│                                                               │
│  Round 2 (Round 1 완료 후):                                   │
│    ├── coupon-service (depends: coupon-model)                │
│    └── ...                                                    │
│                                                               │
│  Round 3 (Round 2 완료 후):                                   │
│    └── coupon-api (depends: coupon-service)                  │
└───────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│  STEP 3: 자율 실행 루프 (핵심)                                │
│                                                               │
│  각 Checkpoint에 대해:                                        │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  3.1 Worker 에이전트 생성                               │ │
│  │                                                         │ │
│  │  • Task 도구로 subagent 생성                           │ │
│  │  • Checkpoint spec + contracts 전달                    │ │
│  │  • 자율 구현 시작                                       │ │
│  └─────────────────────────────────────────────────────────┘ │
│                      │                                        │
│                      ▼                                        │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  3.2 자동 검증 (Checkpoint validation)                  │ │
│  │                                                         │ │
│  │  • 구현 완료 시 validation.command 실행                │ │
│  │  • expected 결과와 비교                                 │ │
│  │  • timeout 내 완료 확인                                 │ │
│  └─────────────────────────────────────────────────────────┘ │
│                      │                                        │
│                      ▼                                        │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  3.3 결과 분기                                          │ │
│  │                                                         │ │
│  │  ✅ 통과 → 다음 Checkpoint 또는 완료                   │ │
│  │  ❌ 실패 → 자동 피드백 생성                            │ │
│  └─────────────────────────────────────────────────────────┘ │
│                      │                                        │
│                      ▼ (실패 시)                              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  3.4 자동 피드백 루프                                   │ │
│  │                                                         │ │
│  │  • 실패 원인 분석                                       │ │
│  │  • 구체적 피드백 생성                                   │ │
│  │  • Worker에게 피드백 전달                               │ │
│  │  • 재시도 (최대 N회)                                    │ │
│  └─────────────────────────────────────────────────────────┘ │
│                      │                                        │
│                      ▼ (최대 재시도 초과)                     │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  3.5 에스컬레이션                                       │ │
│  │                                                         │ │
│  │  • 실패 분석 리포트 생성                                │ │
│  │  • 설계 문제 가능성 제안                                │ │
│  │  • 인간에게 알림                                        │ │
│  └─────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│  STEP 4: 결과 보고                                            │
│                                                               │
│  • 전체 결과 요약                                             │
│  • 성공한 Checkpoint 목록                                     │
│  • 실패한 Checkpoint + 원인 분석                              │
│  • 다음 단계 제안                                             │
└───────────────────────────────────────────────────────────────┘
```

---

## STEP 3: 자율 실행 루프 상세

### 3.1 Worker 에이전트 프롬프트

```markdown
# Implementation Task: {checkpoint-id}

당신은 구현 전문 에이전트입니다. 아래 Checkpoint를 구현해주세요.

## Checkpoint

**이름**: {name}
**설명**: {description}

## 성공 기준 (Criteria)

다음 기준을 **모두** 충족해야 합니다:

{criteria 목록}

## 검증 방법

```bash
{validation.command}
```

예상 결과: `{validation.expected}`

## 참고 자료

### 아키텍처
{architecture.md 내용}

### 인터페이스 계약
{contracts.md 관련 부분}

## 지침

1. 기존 코드 스타일과 패턴을 따르세요
2. 각 criterion을 하나씩 구현하세요
3. 구현 후 검증 명령어를 실행하여 확인하세요
4. 모든 기준이 통과하면 완료입니다

## 제약 사항

- 스펙에 명시되지 않은 기능은 추가하지 마세요
- 기존 테스트가 깨지지 않도록 하세요
- 질문이 있으면 AskUserQuestion을 사용하세요
```

### 3.2 자동 검증 로직

```typescript
async function validateCheckpoint(checkpoint: Checkpoint): Promise<ValidationResult> {
  const { command, expected, timeout } = checkpoint.validation;

  // 검증 명령어 실행
  const result = await runCommand(command, { timeout });

  // 결과 비교
  if (result.stdout.includes(expected)) {
    return {
      passed: true,
      output: result.stdout
    };
  }

  // 실패 분석
  return {
    passed: false,
    output: result.stdout,
    error: result.stderr,
    analysis: analyzeFailure(checkpoint, result)
  };
}
```

### 3.3 자동 피드백 생성

실패 시 구체적인 피드백을 자동 생성합니다.

```markdown
## 🔄 자동 피드백 (Iteration 2/5)

### 실패한 기준

❌ "중복 적용 시 에러 발생"

### 테스트 출력

```
AssertionError: expected 200 to equal 409
  at Context.<anonymous> (test/coupon.test.ts:45:10)
```

### 분석

- 중복 적용 검사 로직이 구현되지 않았습니다
- CouponService.apply() 메서드에서 이미 적용된 쿠폰 체크 필요

### 제안 수정

```typescript
// src/services/coupon.service.ts
async apply(code: string, orderId: string): Promise<ApplyResult> {
  // 추가 필요: 중복 적용 검사
  const existing = await this.couponUsageRepo.findByOrderId(orderId);
  if (existing) {
    throw new ConflictException('쿠폰이 이미 적용되었습니다');
  }
  // ... 기존 로직
}
```

### 다음 단계

위 피드백을 반영하여 재구현해주세요.
```

### 3.4 에스컬레이션 리포트

최대 재시도 초과 시 인간에게 보고합니다.

```markdown
## ⚠️ 에스컬레이션: coupon-service

### 상황

- Checkpoint: coupon-service (쿠폰 서비스 로직)
- 시도 횟수: 3/3 (최대 도달)
- 마지막 실패: 2024-01-15 14:30:00

### 실패 이력

| 시도 | 실패 기준 | 원인 |
|-----|----------|------|
| 1 | validate() 테스트 | null 체크 누락 |
| 2 | 중복 적용 검사 | 트랜잭션 격리 문제 |
| 3 | 중복 적용 검사 | 동시성 race condition |

### 분석

반복적으로 **동시성 관련** 이슈가 발생하고 있습니다.

**가능한 원인:**
1. 설계 단계에서 동시성 요구사항 미정의
2. 트랜잭션 격리 수준 명시 필요
3. 락 전략 결정 필요

### 제안

**설계 재검토 권장:**

```bash
/afl:architect --resume abc12345
```

다음 사항을 명확히 해주세요:
1. 동시에 같은 쿠폰 적용 시 어떻게 처리?
2. 비관적 락 vs 낙관적 락?
3. 재시도 정책은?
```

---

## 파일 구조

```
.afl/
└── sessions/
    └── abc12345/
        └── delegations/
            ├── status.json              # 전체 위임 상태
            │
            └── coupon-service/          # checkpoint-id
                ├── status.json          # 개별 상태
                ├── iterations/
                │   ├── 1/
                │   │   ├── prompt.md    # Worker에게 전달한 프롬프트
                │   │   ├── result.json  # 검증 결과
                │   │   └── feedback.md  # 피드백 (실패 시)
                │   ├── 2/
                │   │   └── ...
                │   └── 3/
                │       └── ...
                │
                └── final-result.json    # 최종 결과
```

### status.json (전체)

```json
{
  "sessionId": "abc12345",
  "status": "in_progress",
  "startedAt": "2024-01-15T12:00:00Z",
  "checkpoints": {
    "coupon-model": { "status": "completed", "iterations": 1 },
    "coupon-service": { "status": "in_progress", "iterations": 2 },
    "coupon-api": { "status": "pending" },
    "coupon-integration": { "status": "pending" }
  },
  "currentRound": 2,
  "totalRounds": 3
}
```

### status.json (개별 Checkpoint)

```json
{
  "checkpointId": "coupon-service",
  "status": "in_progress",
  "iteration": 2,
  "maxIterations": 5,
  "startedAt": "2024-01-15T12:30:00Z",
  "lastAttemptAt": "2024-01-15T13:00:00Z",
  "history": [
    {
      "iteration": 1,
      "result": "failed",
      "failedCriteria": ["validate() 테스트"],
      "timestamp": "2024-01-15T12:45:00Z"
    },
    {
      "iteration": 2,
      "result": "failed",
      "failedCriteria": ["중복 적용 검사"],
      "timestamp": "2024-01-15T13:00:00Z"
    }
  ]
}
```

---

## 출력 예시

### 위임 시작

```
🚀 구현 위임 시작

  세션: abc12345 (쿠폰 할인 기능)

━━━ 실행 계획 ━━━

  Round 1 (병렬):
    • coupon-model - 쿠폰 도메인 모델

  Round 2 (Round 1 완료 후):
    • coupon-service - 쿠폰 서비스 로직

  Round 3 (Round 2 완료 후):
    • coupon-api - 쿠폰 API 엔드포인트

  Round 4 (Round 3 완료 후):
    • coupon-integration - 통합 테스트

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⏳ Round 1 시작...

  [Worker 1] coupon-model 구현 중...
```

### 진행 상황

```
📊 위임 상태: abc12345

━━━ Round 2 진행 중 ━━━

  ✅ coupon-model      완료 (1회 시도)
  🔄 coupon-service    2/5회 시도 중
     └─ 마지막 실패: "중복 적용 검사"
        자동 피드백 반영 후 재시도 중...
  ⏸️ coupon-api        대기 중
  ⏸️ coupon-integration 대기 중

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

상세 로그: .afl/sessions/abc12345/delegations/coupon-service/
```

### 완료 보고

```
✅ 구현 위임 완료: abc12345

━━━ 결과 요약 ━━━

  ✅ coupon-model       1회 시도, 통과
  ✅ coupon-service     3회 시도, 통과
  ✅ coupon-api         1회 시도, 통과
  ✅ coupon-integration 2회 시도, 통과

━━━ 통계 ━━━

  총 시간: 45분
  총 시도: 7회
  성공률: 100% (4/4 Checkpoints)

━━━ 다음 단계 ━━━

  코드 리뷰:
    /team-claude:review --session abc12345

  PR 생성:
    /team-claude:merge --session abc12345
```

### 에스컬레이션

```
⚠️ 에스컬레이션 발생

━━━ 실패한 Checkpoint ━━━

  ❌ coupon-service (3/3회 시도 후 실패)

  반복 실패 원인: 동시성 race condition

━━━ 분석 ━━━

  설계 단계에서 동시성 요구사항이 명확하지 않습니다.

  가능한 원인:
  1. 락 전략 미정의
  2. 트랜잭션 격리 수준 미명시

━━━ 권장 조치 ━━━

  설계 재검토:
    /afl:architect --resume abc12345

  수동 구현:
    /team-claude:spawn coupon-service

상세 분석: .afl/sessions/abc12345/delegations/coupon-service/
```

---

## 설정

```json
{
  "delegation": {
    "autoValidateOnComplete": true,
    "autoRetryOnFail": true,
    "maxRetries": 3,
    "retryDelay": 5000,
    "parallelWorkers": 3,
    "escalationThreshold": 3
  }
}
```

| 설정 | 설명 | 기본값 |
|-----|------|--------|
| `autoValidateOnComplete` | 구현 완료 시 자동 검증 | `true` |
| `autoRetryOnFail` | 실패 시 자동 재시도 | `true` |
| `maxRetries` | 최대 재시도 횟수 | `3` |
| `retryDelay` | 재시도 간 대기 시간 (ms) | `5000` |
| `parallelWorkers` | 동시 실행 Worker 수 | `3` |
| `escalationThreshold` | 에스컬레이션 트리거 횟수 | `3` |
