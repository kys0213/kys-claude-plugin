> ❄️ **Snapshot freeze** — 이 플러그인은 v0.7.1 에서 동결되었습니다.
> 후속 개발은 [atelier](../atelier/) 에서 진행됩니다.
> 마이그레이션: `plugins/atelier/README.md` 참조.

# spec-kit

스펙 설계, 검증, 갭 분석을 위한 통합 툴킷.

agents/templates 는 atelier 의 `agents/spec/`, `templates/spec/` 으로, 커맨드 로직은
`spec` skill 로 이전되었습니다. `spec-validator` agent 는 `gap-auditor` 로 통합되었습니다.
