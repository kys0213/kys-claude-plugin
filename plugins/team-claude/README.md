# Team Claude Plugin

멀티 에이전트 협업 시스템: Contract 기반 설계, Worker 자율 RALPH, Semi-Auto 머지

## Quick Start

```bash
# 1. 서버 설정 및 실행
/team-claude:setup server

# 2. 설계 시작 (인간-에이전트 협업)
/team-claude:architect "쿠폰 할인 기능 개발"

# 3. 구현 위임 (자율 에이전트)
/team-claude:delegate --all

# 4. 머지 (Semi-Auto)
/team-claude:merge
```

---

## 핵심 원칙

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│  1. 사람 + Main Agent 협업                                                  │
│     • Architect: 설계, Contract + Test Code 정의                            │
│     • Conflict: 사용자 최종 판단                                            │
│                                                                             │
│  2. Worker 자율 완결 (RALPH)                                                │
│     • 서버 개입 없이 내부에서 피드백 루프                                   │
│     • Context 관리도 자체 처리 (/clear + checkpoint)                        │
│     • 완료 시 PR 생성                                                       │
│                                                                             │
│  3. Contract 기반 병렬화                                                    │
│     • Interface + Test Code = Contract                                      │
│     • 구현에 의존하지 않음, 계약에만 의존                                   │
│     • 독립 테스트 가능 → 병렬 실행 가능                                     │
│                                                                             │
│  4. Epic 브랜치 전략                                                        │
│     • main → epic/{feature} → team-claude/{task}                                    │
│     • Worker PR → epic 머지 → main PR                                       │
│                                                                             │
│  5. Semi-Auto 머지                                                          │
│     • 기본: 자동 머지                                                       │
│     • Conflict: 분석 Agent + 사용자 선택                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 전체 워크플로우

```
═══════════════════════════════════════════════════════════════════════════════
Phase 0: 테스트 환경 구축 (필요시)
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│  공유 테스트 환경이 필요한 경우 먼저 구축                                   │
│                                                                             │
│  • DB 스키마 마이그레이션                                                   │
│  • 테스트용 fixtures                                                        │
│  • Docker compose 환경                                                      │
│                                                                             │
│  이 작업은 병렬 Worker 실행 전에 완료되어야 함                              │
└─────────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
Phase 1: ARCHITECT (Main Agent + 사용자 협업)
═══════════════════════════════════════════════════════════════════════════════

사용자: "쿠폰 할인 기능 만들어줘"
        │
        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  /team-claude:architect                                                             │
│                                                                             │
│  Main Agent가 사용자와 대화하며:                                            │
│                                                                             │
│  1. 요구사항 분석                                                           │
│  2. 아키텍처 설계                                                           │
│  3. Task 분할                                                               │
│  4. 각 Task별 Contract 정의:                                                │
│     • Interface (타입, API 스펙)                                            │
│     • Test Code (검증 테스트 - TDD 방식)                                    │
│  5. 의존성 그래프 작성                                                      │
│                                                                             │
│  산출물:                                                                    │
│  .team-claude/sessions/{session-id}/                                                │
│    ├── architecture.md                                                      │
│    ├── contracts/                                                           │
│    │   ├── coupon-model/                                                    │
│    │   │   ├── interface.ts        # 타입 정의                              │
│    │   │   └── contract.test.ts    # 검증 테스트 (미리 작성!)               │
│    │   ├── coupon-service/                                                  │
│    │   │   ├── interface.ts                                                 │
│    │   │   └── contract.test.ts                                             │
│    │   └── coupon-api/                                                      │
│    │       ├── openapi.yaml                                                 │
│    │       └── contract.test.ts                                             │
│    └── checkpoints/                                                         │
│        ├── coupon-model.json                                                │
│        ├── coupon-service.json                                              │
│        └── coupon-api.json                                                  │
└─────────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Contract = Interface + Test Code                                           │
│                                                                             │
│  # tests/contracts/test_coupon_model_contract.py                            │
│  # Worker가 이 테스트를 통과해야 Task 완료                                  │
│                                                                             │
│  def test_coupon_entity_has_required_fields():                              │
│      coupon = Coupon(code="SUMMER2024", discount_type="percent", ...)       │
│      assert coupon.code == "SUMMER2024"                                     │
│                                                                             │
│  def test_coupon_repository_save_and_find():                                │
│      repo = CouponRepository()                                              │
│      repo.save(coupon)                                                      │
│      found = repo.find_by_code("SUMMER2024")                                │
│      assert found is not None                                               │
└─────────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
Phase 2: DELEGATE (Main Agent → Workers 위임)
═══════════════════════════════════════════════════════════════════════════════

사용자: "좋아, 구현해줘" 또는 /team-claude:delegate --all
        │
        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Main Agent: Epic 브랜치 생성                                               │
│                                                                             │
│  git checkout -b epic/coupon-feature                                        │
│                                                                             │
│  main                                                                       │
│    └── epic/coupon-feature  ← 모든 Worker PR이 여기로 머지됨                │
└─────────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  Main Agent: 각 Task별 Worktree + CLAUDE.md 생성                            │
│                                                                             │
│  git worktree add .team-claude/worktrees/coupon-model -b team-claude/coupon-model           │
│                                                                             │
│  .team-claude/worktrees/coupon-model/                                               │
│    ├── CLAUDE.md              ← Worker 지시서 + RALPH 루프 지침             │
│    ├── .claude/                                                             │
│    │   └── settings.local.json  ← hooks 설정 (context 감시)                 │
│    └── (프로젝트 파일들)                                                    │
└─────────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  서버가 iTerm 새 탭에서 Worker 실행                                         │
│                                                                             │
│  각 Worker는 독립적으로 자율 실행:                                          │
│  - 사용자가 iTerm에서 실시간 확인 가능                                      │
│  - 각 탭이 하나의 Task 담당                                                 │
└─────────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
Phase 3: WORKER (자율 RALPH 루프 - 서버 개입 없음!)
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│  Worker Claude (iTerm 새 탭에서 독립 실행)                                  │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                         자율 RALPH 루프                               │ │
│  │                                                                       │ │
│  │  ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐        │ │
│  │  │ 구현    │ ──► │ 검증    │ ──► │ 분석    │ ──► │ 수정    │        │ │
│  │  │         │     │(Contract│     │ (실패시)│     │         │        │ │
│  │  │         │     │  Test)  │     │         │     │         │        │ │
│  │  └─────────┘     └─────────┘     └─────────┘     └────┬────┘        │ │
│  │       ▲                                               │              │ │
│  │       └───────────────────────────────────────────────┘              │ │
│  │                        (통과할 때까지 반복)                           │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
│  Worker가 스스로:                                                           │
│  • Contract Test 실행 + 실패 분석 + 재시도                                  │
│  • Context 80% → checkpoint + /clear                                        │
│  • 성공 시 PR 생성 (→ epic 브랜치로)                                        │
│                                                                             │
│  서버 개입 없음 - Worker가 자율적으로 완결                                  │
└─────────────────────────────────────────────────────────────────────────────┘


  Context 80% 도달 시:
  ┌─────────────────────────────────────────────────────────────────────────┐
  │  Statusline hook이 context % 감지                                       │
  │        │                                                                │
  │        ▼                                                                │
  │  Worker가:                                                              │
  │    1. .team-claude-checkpoint.md 생성 (진행 상황 기록)                          │
  │    2. /clear 실행                                                       │
  │    3. CLAUDE.md + checkpoint 읽고 이어서 작업                           │
  │                                                                         │
  │  또는 TodoWrite 활용 시:                                                │
  │    1. TodoWrite로 상태 유지                                             │
  │    2. /compact 실행                                                     │
  │    3. todo 기반으로 이어서 작업                                         │
  └─────────────────────────────────────────────────────────────────────────┘


  완료 시:
  ┌─────────────────────────────────────────────────────────────────────────┐
  │  Worker: PR 생성                                                        │
  │                                                                         │
  │  gh pr create \                                                         │
  │    --base epic/coupon-feature \                                         │
  │    --head team-claude/coupon-model \                                            │
  │    --title "feat(coupon): implement Coupon model"                       │
  │                                                                         │
  │  .team-claude-result.json:                                                      │
  │  { "status": "completed", "pr_url": "...", "attempts": 2 }              │
  └─────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
Phase 4: MERGE (Main Agent - Semi-Auto)
═══════════════════════════════════════════════════════════════════════════════

모든 Worker PR 완료
        │
        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  /team-claude:merge                                                                 │
│                                                                             │
│  Main Agent: 순차적 머지 시도                                               │
│                                                                             │
│  PR 1 (coupon-model)                                                        │
│    └── ✅ 자동 머지 성공                                                    │
│                                                                             │
│  PR 2 (coupon-service)                                                      │
│    └── ⚠️ CONFLICT 발생!                                                    │
│          │                                                                  │
│          ▼                                                                  │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  Conflict Analysis Agent                                              │ │
│  │                                                                       │ │
│  │  각 conflict에 대해:                                                  │ │
│  │  1. git log로 양쪽 변경 이력 분석                                     │ │
│  │  2. 연결된 코드 탐색 (호출부, 의존성)                                 │ │
│  │  3. 양쪽 의도 추론                                                    │ │
│  │  4. 해결 방안 제시                                                    │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│          │                                                                  │
│          ▼                                                                  │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │  Main Agent → 사용자에게 질문                                         │ │
│  │                                                                       │ │
│  │  AskUserQuestion:                                                     │ │
│  │  "src/coupon.ts:45 충돌 해결 방법을 선택하세요"                       │ │
│  │                                                                       │ │
│  │  ○ 분석 결과 적용 (권장)                                              │ │
│  │  ○ coupon-model 버전 유지                                             │ │
│  │  ○ coupon-service 버전 유지                                           │ │
│  │  ○ 직접 수정                                                          │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│          │                                                                  │
│          ▼                                                                  │
│  사용자 선택 → 해결 적용 → 다음 conflict / 다음 PR                          │
│                                                                             │
│  PR 3 (coupon-api)                                                          │
│    └── ✅ 자동 머지 성공                                                    │
└─────────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  완료: epic/coupon-feature → main PR 생성                                   │
│                                                                             │
│  gh pr create --base main --head epic/coupon-feature                        │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Checkpoint 구조

```json
{
  "id": "coupon-model",
  "name": "쿠폰 도메인 모델",
  "description": "Coupon 엔티티와 Repository 구현",

  "contract": {
    "interface": ".team-claude/sessions/.../contracts/coupon-model/interface.ts",
    "tests": "tests/contracts/test_coupon_model_contract.py"
  },

  "validation": {
    "command": "pytest tests/contracts/test_coupon_model_contract.py -v",
    "expected": "passed"
  },

  "prerequisites": {
    "environment": ["db-setup"],
    "tasks": []
  },

  "dependencies": []
}
```

---

## 파일 구조

```
.team-claude/
├── sessions/
│   └── {session-id}/
│       ├── architecture.md           # 전체 설계
│       ├── contracts/                 # Contract 정의
│       │   ├── coupon-model/
│       │   │   ├── interface.ts       # 타입/인터페이스
│       │   │   └── contract.test.ts   # 검증 테스트
│       │   └── coupon-service/
│       │       └── ...
│       └── checkpoints/               # Task 정의
│           ├── coupon-model.json
│           └── coupon-service.json
│
└── worktrees/                         # Git Worktree (Worker 작업공간)
    ├── coupon-model/
    │   ├── CLAUDE.md                  # Worker 지시서
    │   ├── .claude/
    │   │   └── settings.local.json    # Hooks 설정
    │   ├── .team-claude-checkpoint.md         # Context 관리용
    │   ├── .team-claude-result.json           # 완료 결과
    │   └── (프로젝트 파일들)
    └── coupon-service/
        └── ...
```

---

## 명령어 요약

| 명령어 | 용도 | 인간 개입 |
|--------|------|----------|
| `/team-claude:setup` | 서버 설정 및 실행 | 초기 1회 |
| `/team-claude:architect` | 설계 + Contract + Test Code 작성 | **적극 참여** |
| `/team-claude:delegate` | Worker에게 구현 위임 | 시작만 |
| `/team-claude:merge` | PR 머지 (conflict 시 사용자 확인) | **conflict 시 참여** |
| `/team-claude:loop-status` | 진행 상황 확인 | 모니터링 |

---

## 브랜치 전략

```
main
  │
  └── epic/coupon-feature         ← Main Agent가 생성/관리
        │
        ├── team-claude/coupon-model      ← Worker 1 (PR → epic)
        ├── team-claude/coupon-service    ← Worker 2 (PR → epic)
        └── team-claude/coupon-api        ← Worker 3 (PR → epic)
              │
              ▼
        모든 PR 머지 후
        epic → main PR 생성
```

---

## Worker RALPH 지침 (CLAUDE.md에 포함)

```markdown
## 🔄 RALPH: 자율 피드백 루프

### 실행 루프

repeat:
  1. 구현 (Success Criteria 기준)
  2. Contract Test 실행 (validation command)
  3. 결과 확인
     - ✅ 통과 → PR 생성 후 종료
     - ❌ 실패 → 에러 분석 후 수정
until 통과 or 최대 재시도 초과

### Context 80% 도달 시

Hook에서 경고 메시지가 오면:
1. .team-claude-checkpoint.md에 진행 상황 저장
2. /clear 실행
3. CLAUDE.md + checkpoint 읽고 이어서 작업

### 완료 시

1. 변경사항 커밋
2. PR 생성: gh pr create --base epic/{feature} --head team-claude/{task}
3. .team-claude-result.json 생성
```

---

## 설정 파일

```yaml
# .claude/team-claude.yaml

project:
  language: python
  framework: fastapi
  test_command: pytest

server:
  port: 7890
  executor: iterm          # iterm | terminal-app | headless

feedback_loop:
  max_iterations: 3
  context_checkpoint_threshold: 80   # Context % 임계값

merge:
  auto_merge: true         # conflict 없으면 자동 머지
  conflict_strategy: ask   # ask | ours | theirs
```
