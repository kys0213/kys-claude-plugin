---
name: team-claude:swarm
description: Swarm Mode - 여러 서브에이전트에게 태스크를 병렬로 위임
argument-hint: "<tasks...> | --from-checkpoints <session-id>"
allowed-tools: ["Task", "Bash", "Read", "Write", "Glob", "Grep", "Edit"]
---

# Swarm Command - 병렬 에이전트 위임

> **먼저 읽기**: `${CLAUDE_PLUGIN_ROOT}/INFRASTRUCTURE.md`

worktree 없이 여러 서브에이전트에게 태스크를 병렬로 위임합니다.

---

## 핵심 개념

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  SWARM MODE: 내부 병렬 에이전트                                              │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Main Agent (Orchestrator)                     │   │
│  │                                                                      │   │
│  │  1. 태스크 분할                                                      │   │
│  │  2. 독립성 검증 (파일 충돌 체크)                                     │   │
│  │  3. 서브에이전트 병렬 실행                                           │   │
│  │  4. 결과 수집 및 통합                                                │   │
│  └──────────────────────────┬──────────────────────────────────────────┘   │
│                              │                                              │
│           ┌──────────────────┼──────────────────┐                          │
│           │                  │                  │                          │
│           ▼                  ▼                  ▼                          │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐                    │
│  │ SubAgent 1  │    │ SubAgent 2  │    │ SubAgent 3  │                    │
│  │             │    │             │    │             │                    │
│  │ Task A      │    │ Task B      │    │ Task C      │                    │
│  │ (model.ts)  │    │ (service.ts)│    │ (api.ts)    │                    │
│  └──────┬──────┘    └──────┬──────┘    └──────┬──────┘                    │
│         │                  │                  │                            │
│         ▼                  ▼                  ▼                            │
│      Result A           Result B           Result C                        │
│         │                  │                  │                            │
│         └──────────────────┼──────────────────┘                            │
│                            ▼                                                │
│                   ┌─────────────┐                                          │
│                   │   Merge     │                                          │
│                   │   Results   │                                          │
│                   └─────────────┘                                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

### PSM vs Swarm 비교

| 특성 | PSM (worktree) | Swarm (내부) |
|------|---------------|--------------|
| 격리 수준 | 완전 격리 (별도 디렉토리) | 동일 코드베이스 |
| 브랜치 | 각각 별도 브랜치 | 같은 브랜치 |
| 파일 충돌 | 불가능 | 주의 필요 |
| 리소스 | 디스크 많이 사용 | 가벼움 |
| 적합한 경우 | 독립적인 큰 기능 | 작은 태스크, 같은 기능 내 분할 |

---

## 사용법

```bash
# 태스크 목록으로 실행
/team-claude:swarm "coupon model 구현" "coupon service 구현" "coupon API 구현"

# 세션의 checkpoints에서 가져오기
/team-claude:swarm --from-checkpoints abc12345

# Magic Keyword
swarm: model, service, api 구현

# 최대 병렬 수 제한
/team-claude:swarm --max-parallel 3 "task1" "task2" "task3" "task4"
```

---

## 실행 절차

```
/team-claude:swarm "task1" "task2" "task3"
        │
        ▼
┌───────────────────────────────────────────────────────────────────┐
│  STEP 1: 태스크 분석 및 독립성 검증                               │
│                                                                   │
│  각 태스크가 수정할 파일 예측:                                    │
│  • task1: src/models/coupon.ts                                   │
│  • task2: src/services/coupon.service.ts                         │
│  • task3: src/api/coupon.controller.ts                           │
│                                                                   │
│  충돌 검사:                                                       │
│  • ✅ 파일 겹침 없음 → 병렬 실행 가능                            │
│  • ⚠️ 파일 겹침 발견 → 순차 실행 또는 분할 조정                  │
└───────────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────────┐
│  STEP 2: 서브에이전트 병렬 실행                                   │
│                                                                   │
│  Task tool을 사용하여 병렬 호출:                                  │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │  <parallel>                                                  │ │
│  │    Task(subagent_type="general-purpose", prompt="task1")     │ │
│  │    Task(subagent_type="general-purpose", prompt="task2")     │ │
│  │    Task(subagent_type="general-purpose", prompt="task3")     │ │
│  │  </parallel>                                                 │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                   │
│  각 서브에이전트는:                                               │
│  • 지정된 파일만 수정                                            │
│  • Contract/Test 기반 구현                                       │
│  • 결과 반환                                                     │
└───────────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────────┐
│  STEP 3: 결과 수집 및 통합                                        │
│                                                                   │
│  모든 서브에이전트 완료 후:                                       │
│  • 각 결과 검토                                                  │
│  • 충돌 있으면 해결                                              │
│  • 통합 테스트 실행                                              │
│  • 커밋 생성                                                     │
└───────────────────────────────────────────────────────────────────┘
```

---

## 서브에이전트 프롬프트 템플릿

```markdown
## Task: {task_name}

### Context
당신은 Swarm의 서브에이전트입니다. 다음 태스크를 수행하세요.

### Constraints
- **수정 가능 파일**: {allowed_files}
- **수정 금지 파일**: {forbidden_files}
- 다른 서브에이전트가 동시에 작업 중이므로 지정된 파일만 수정하세요.

### Contract
{contract_interface}

### Test
```
{contract_test}
```

### Success Criteria
{success_criteria}

### Instructions
1. Contract에 맞게 구현
2. Test가 통과하도록 작성
3. 기존 코드 스타일 유지
4. 완료 후 변경 내용 요약 반환
```

---

## 충돌 방지 전략

### 1. 파일 기반 분할

```yaml
# 태스크별 파일 할당
tasks:
  - name: coupon-model
    allowed_files:
      - src/models/coupon.ts
      - src/models/coupon.types.ts
    forbidden_files:
      - src/services/**
      - src/api/**

  - name: coupon-service
    allowed_files:
      - src/services/coupon.service.ts
      - src/services/coupon.repository.ts
    forbidden_files:
      - src/models/**
      - src/api/**

  - name: coupon-api
    allowed_files:
      - src/api/coupon.controller.ts
      - src/api/coupon.dto.ts
    forbidden_files:
      - src/models/**
      - src/services/**
```

### 2. 공유 파일 처리

```
공유 파일 발견 시:
1. index.ts, types.ts 등 공유 파일 식별
2. 선택:
   a) 한 에이전트만 수정 (나머지는 읽기만)
   b) 순차 실행으로 전환
   c) 공유 파일은 마지막에 Main Agent가 통합
```

### 3. 의존성 순서

```
의존성 있는 경우:
Round 1: 독립 태스크 (병렬)
  └── model, constants

Round 2: Round 1에 의존 (병렬)
  └── service, repository

Round 3: Round 2에 의존 (병렬)
  └── controller, api
```

---

## 사용 시나리오

### 시나리오 1: 기능 내 분할

```bash
# 쿠폰 기능을 레이어별로 분할
/team-claude:swarm \
  "Coupon 엔티티와 타입 정의 (src/models/)" \
  "CouponService 비즈니스 로직 (src/services/)" \
  "Coupon API 엔드포인트 (src/api/)"
```

### 시나리오 2: 테스트 병렬 작성

```bash
# 여러 테스트 파일 병렬 작성
/team-claude:swarm \
  "CouponModel unit tests" \
  "CouponService unit tests" \
  "CouponAPI integration tests"
```

### 시나리오 3: 리팩토링 병렬 수행

```bash
# 여러 파일 동시 리팩토링
/team-claude:swarm \
  "UserService 리팩토링" \
  "OrderService 리팩토링" \
  "PaymentService 리팩토링"
```

### 시나리오 4: Checkpoints 기반

```bash
# architect에서 정의한 checkpoints 활용
/team-claude:architect "쿠폰 기능"
# → checkpoints: coupon-model, coupon-service, coupon-api

/team-claude:swarm --from-checkpoints abc12345
# → 3개 checkpoint를 병렬 서브에이전트로 실행
```

---

## 실행 예시

```
🐝 Swarm 모드 시작

  태스크: 3개
  병렬 실행: 최대 4개

━━━ 태스크 분석 ━━━

  ✅ coupon-model     → src/models/coupon.ts
  ✅ coupon-service   → src/services/coupon.service.ts
  ✅ coupon-api       → src/api/coupon.controller.ts

  충돌 검사: ✅ 없음 (병렬 실행 가능)

━━━ 서브에이전트 실행 ━━━

  [1/3] coupon-model     🔄 실행 중...
  [2/3] coupon-service   🔄 실행 중...
  [3/3] coupon-api       🔄 실행 중...

  [1/3] coupon-model     ✅ 완료 (45초)
  [2/3] coupon-service   ✅ 완료 (52초)
  [3/3] coupon-api       ✅ 완료 (38초)

━━━ 결과 통합 ━━━

  변경된 파일:
    + src/models/coupon.ts
    + src/models/coupon.types.ts
    + src/services/coupon.service.ts
    + src/api/coupon.controller.ts
    + src/api/coupon.dto.ts

  통합 테스트: ✅ 통과

━━━ 완료 ━━━

  총 소요 시간: 1분 12초
  (순차 실행 예상: 2분 15초)
```

---

## 설정

```yaml
# .claude/team-claude.yaml
swarm:
  enabled: true
  maxParallel: 4              # 최대 동시 서브에이전트 수

  conflictCheck:
    enabled: true
    action: warn              # warn | block | sequential

  # 공유 파일 정책
  sharedFiles:
    patterns:
      - "**/index.ts"
      - "**/types.ts"
      - "**/*.d.ts"
    policy: last_merge        # last_merge | first_only | sequential

  # 서브에이전트 설정
  subagent:
    type: general-purpose
    timeout: 300000           # 5분
    retries: 2
```

---

## Flow와 통합

```bash
# Flow 명령에서 swarm 모드 사용
/team-claude:flow "기능 설명" --mode autopilot --impl-strategy swarm

# 또는 Magic Keyword
autopilot+swarm: 쿠폰 기능 추가
```

```
Flow에서 IMPL 단계:
  --impl-strategy psm    → git worktree 기반 (기본값)
  --impl-strategy swarm  → 내부 서브에이전트 병렬
  --impl-strategy sequential → 순차 실행
```
