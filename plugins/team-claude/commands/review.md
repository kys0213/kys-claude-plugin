---
name: team-claude:review
description: 완료된 Task 코드 리뷰 - 에이전트 기반 자동 리뷰 및 피드백 생성
argument-hint: "<task-id> [--agents=<agent-list>]"
allowed-tools: ["Task", "Read", "Bash", "Glob", "Grep", "Write"]
---

# Team Claude 리뷰 커맨드

완료된 Task의 코드를 에이전트를 통해 리뷰합니다.

## 사용법

```bash
# 기본 리뷰 (설정된 모든 에이전트)
/team-claude:review task-coupon-service

# 특정 에이전트만 사용
/team-claude:review task-coupon-service --agents=security-auditor,qa-agent
```

## Arguments

| Argument | 필수 | 설명 |
|----------|------|------|
| task-id | O | 리뷰할 Task ID |
| --agents | X | 특정 에이전트만 사용 (쉼표 구분) |

---

## 리뷰 프로세스

```
/team-claude:review task-coupon-service
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│                     1. Task 상태 확인                          │
│                                                               │
│  • Task가 completed 상태인지 확인                             │
│  • 이미 리뷰된 경우 재리뷰 여부 확인                           │
└───────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│                     2. 변경 사항 수집                          │
│                                                               │
│  • git diff main...feature/task-coupon-service               │
│  • 변경된 파일 내용 수집                                       │
│  • 관련 Contract/테스트 파일 수집                              │
└───────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│                     3. 에이전트 리뷰 실행                       │
│                                                               │
│  Task(subagent_type="code-reviewer", run_in_background=true) │
│  Task(subagent_type="qa-agent", run_in_background=true)      │
│  Task(subagent_type="security-auditor", run_in_background=true)│
└───────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│                     4. 피드백 분류                             │
│                                                               │
│  • auto-fixable: 자동 수정 가능한 항목                        │
│  • suggestion: 권장 사항                                      │
│  • blocking: 반드시 수정 필요                                  │
│  • approved: 승인                                             │
└───────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│                     5. 결과 보고                               │
│                                                               │
│  • 리뷰 결과 출력                                              │
│  • 자동 수정 제안                                              │
│  • 다음 단계 안내                                              │
└───────────────────────────────────────────────────────────────┘
```

---

## Step 1: Task 상태 확인

### 완료 확인

```
❌ Task가 아직 완료되지 않았습니다: task-coupon-service

현재 상태: running (60%)

완료 후 다시 시도해주세요.
상태 확인: /team-claude:status task-coupon-service
```

### 재리뷰 확인

```
⚠️ 이미 리뷰된 Task입니다: task-coupon-service

이전 리뷰: 2024-01-15 11:00:00
결과: ✅ 승인 (권장사항 1개)

다시 리뷰하시겠습니까?
  1. 예, 다시 리뷰
  2. 이전 리뷰 결과 보기
  3. 취소
```

---

## Step 2: 변경 사항 수집

### Git diff 수집

```bash
cd ../worktrees/task-coupon-service
git diff main...HEAD --name-status
```

### 파일 분류

| 타입 | 설명 | 리뷰 중점 |
|------|------|----------|
| A (Added) | 신규 파일 | 전체 리뷰 |
| M (Modified) | 수정된 파일 | 변경 부분 집중 |
| D (Deleted) | 삭제된 파일 | 영향도 분석 |

### 컨텍스트 수집

리뷰에 필요한 추가 파일들:

- `.team-claude/specs/contracts/` - Contract 정의
- `.team-claude/specs/tasks/task-coupon-service.md` - Task 스펙
- 관련 테스트 파일

---

## Step 3: 에이전트 리뷰 실행

### 병렬 실행

```
3개 에이전트 리뷰 시작...

  🔄 code-reviewer: 실행 중
  🔄 qa-agent: 실행 중
  🔄 security-auditor: 실행 중
```

### 각 에이전트 역할

| 에이전트 | 검토 항목 |
|---------|----------|
| Code Reviewer | 코드 품질, 컨벤션, 중복, 복잡도 |
| QA Agent | 테스트 커버리지, 엣지 케이스, 에러 처리 |
| Security Auditor | 인증, 권한, 입력 검증, 보안 취약점 |
| Domain Expert | 도메인 로직 정확성, 비즈니스 규칙 |
| Performance Analyst | 성능 영향, N+1 쿼리, 메모리 |

---

## Step 4: 피드백 분류

### 분류 기준

```typescript
interface ReviewFeedback {
  type: "auto-fixable" | "suggestion" | "blocking" | "approved";
  agent: string;
  file: string;
  line?: number;
  message: string;
  fix?: string;  // auto-fixable인 경우 수정 코드
}
```

### 예시

```json
[
  {
    "type": "auto-fixable",
    "agent": "code-reviewer",
    "file": "src/services/coupon.service.ts",
    "line": 45,
    "message": "unused import: lodash",
    "fix": "// import removed"
  },
  {
    "type": "suggestion",
    "agent": "security-auditor",
    "file": "src/services/coupon.service.ts",
    "line": 78,
    "message": "rate limiting 추가 권장"
  },
  {
    "type": "approved",
    "agent": "qa-agent",
    "message": "테스트 커버리지 87%, 모든 시나리오 커버됨"
  }
]
```

---

## Step 5: 결과 보고

### 리뷰 결과 출력

```
🔍 Task-A (CouponService) 리뷰 결과

📁 변경 사항:
  +src/services/coupon.service.ts (신규, 156줄)
  +src/services/coupon.service.test.ts (신규, 234줄)
  ~src/types/index.ts (수정, +15줄)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🤖 에이전트 리뷰:

  Code Reviewer:
    ✅ 코드 구조 명확
    ✅ 네이밍 컨벤션 준수
    ⚠️ [L45] unused import 제거 필요
       → 자동 수정 가능

  QA Agent:
    ✅ 테스트 커버리지 87%
    ✅ 모든 에러 케이스 테스트됨
    ✅ 동시성 테스트 포함

  Security Auditor:
    ✅ 입력 검증 적절
    ⚠️ [L78] rate limiting 추가 권장
       → 자동 수정 가능
    ✅ SQL injection 방지됨

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 요약:
  ✅ 승인: 3
  ⚠️ 권장: 2 (자동 수정 가능: 2)
  ❌ 차단: 0

자동 수정을 적용할까요? [Y/n]
```

### 자동 수정 적용

```
🔧 자동 수정 적용 중...

  ✅ [L45] unused import 제거됨
  ✅ [L78] rate limiting 추가됨

수정 커밋 생성됨: fix(coupon): apply review suggestions

다음 단계:
  머지: /team-claude:merge task-coupon-service
  추가 피드백: /team-claude:feedback task-coupon-service "..."
```

### 차단 항목이 있는 경우

```
🔍 Task-A (CouponService) 리뷰 결과

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❌ 차단 항목 발견 - 수정이 필요합니다

  Security Auditor:
    ❌ [L92] 하드코딩된 시크릿 발견
       → 환경 변수로 이동 필요

    ❌ [L105] SQL injection 취약점
       → parameterized query 사용 필요

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 요약:
  ✅ 승인: 2
  ⚠️ 권장: 1
  ❌ 차단: 2

머지 불가: 차단 항목을 먼저 해결해주세요.

Worker에 피드백 전달:
  /team-claude:feedback task-coupon-service "보안 이슈 수정 필요"
```

---

## 리뷰 저장

리뷰 결과는 저장되어 추적됩니다:

```
.team-claude/reviews/
└── task-coupon-service/
    ├── review-2024-01-15-110000.json
    └── review-2024-01-15-113000.json  # 재리뷰
```

---

## 에러 처리

### Task 없음

```
❌ Task를 찾을 수 없습니다: unknown-task

완료된 Task:
  - task-coupon-service
  - task-admin-ui
```

### 에이전트 없음

```
❌ 에이전트를 찾을 수 없습니다: invalid-agent

사용 가능한 에이전트:
  - code-reviewer
  - qa-agent
  - security-auditor
  - domain-expert
```
