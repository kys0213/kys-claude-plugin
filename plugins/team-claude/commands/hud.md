---
name: team-claude:hud
description: HUD (Heads-Up Display) - 워크플로우 상태를 statusline에 표시
argument-hint: "setup | config | reset"
allowed-tools: ["Bash", "Read", "Write", "Edit"]
---

# HUD Command - 워크플로우 상태 표시

> Team Claude 워크플로우 상태를 터미널 statusline에 실시간 표시

---

## 핵심 기능

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  Team Claude HUD                                                             │
│                                                                              │
│  🚀 autopilot │ 📋 spec ████████░░ 80% │ 🐝 swarm 3/4 │ 🔄 review 2/5      │
│                                                                              │
│  표시 항목:                                                                  │
│  • 모드 (autopilot/assisted/manual)                                         │
│  • 현재 단계 (spec/impl/merge) + 진행률                                     │
│  • 구현 전략 (psm/swarm/sequential)                                         │
│  • Auto-review 상태                                                         │
│  • PSM 세션 상태                                                            │
│  • Swarm 에이전트 상태                                                      │
│  • RALPH 루프 반복 횟수                                                     │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 사용법

```bash
# HUD 설정 (대화형)
/team-claude:hud setup

# 설정 변경
/team-claude:hud config

# 설정 초기화
/team-claude:hud reset

# 직접 statusline 스크립트 실행 (테스트용)
${CLAUDE_PLUGIN_ROOT}/scripts/tc-hud.sh
```

---

## 설정

### Preset 선택

```
/team-claude:hud setup

━━━ Team Claude HUD Setup ━━━

  프리셋 선택:

  1. 🎯 Full      모든 정보 표시
  2. 📊 Essential 핵심 정보만 (기본)
  3. 🔹 Minimal   최소 정보

  선택 (1-3): _
```

### 개별 설정

```yaml
# ~/.claude/team-claude-hud.yaml
hud:
  enabled: true
  preset: essential

  # 표시 항목
  display:
    mode: true           # 실행 모드
    phase: true          # 현재 단계
    progress: true       # 진행률 바
    implStrategy: true   # 구현 전략
    review: true         # 리뷰 상태
    psm: true            # PSM 세션
    swarm: true          # Swarm 에이전트
    ralph: true          # RALPH 반복
    duration: true       # 경과 시간

  # 스타일
  style:
    separator: " │ "
    progressBar:
      width: 10
      filled: "█"
      empty: "░"
    icons:
      autopilot: "🚀"
      assisted: "👤"
      manual: "✋"
      spec: "📋"
      impl: "🔧"
      merge: "🔀"
      psm: "🌳"
      swarm: "🐝"
      review: "🔍"
      ralph: "🔄"
```

---

## HUD 출력 형식

### Full Preset

```
🚀 autopilot │ 📋 spec ████████░░ 80% │ 🐝 swarm │ 🔍 review 2/5 │ 🔄 ralph 3 │ ⏱️ 5m23s
```

### Essential Preset

```
🚀 auto │ spec 80% │ 🐝 3/4 │ 5m
```

### Minimal Preset

```
🚀 spec 80%
```

---

## 단계별 표시

### SPEC 단계

```
📋 spec ████████░░ 80%
   └── 요구사항 분석 완료, 아키텍처 설계 중

📋 spec-review 2/5
   └── 자동 리뷰 2회차 진행 중
```

### IMPL 단계 - PSM

```
🌳 psm 2/3 세션
   ├── coupon-model ✅
   ├── coupon-service 🔄 ralph 2
   └── coupon-api ⏸️
```

### IMPL 단계 - Swarm

```
🐝 swarm 3/4
   ├── [1] model ✅
   ├── [2] service 🔄
   ├── [3] api 🔄
   └── [4] test ⏸️
```

### MERGE 단계

```
🔀 merge 2/3
   ├── PR #123 ✅
   ├── PR #124 ⚠️ conflict
   └── PR #125 ⏸️
```

---

## 설치 방법

### 1. Statusline 스크립트 설치

```bash
# 스크립트 복사
cp ${CLAUDE_PLUGIN_ROOT}/scripts/tc-hud.sh ~/.claude/tc-hud.sh
chmod +x ~/.claude/tc-hud.sh
```

### 2. Claude Code 설정

```json
// ~/.claude/settings.json
{
  "statusLine": {
    "type": "command",
    "command": "~/.claude/tc-hud.sh",
    "padding": 0
  }
}
```

### 3. 또는 기존 statusline과 통합

```bash
# ~/.claude/statusline.sh

#!/bin/bash

# 기존 statusline 출력
existing_status=$(your_existing_statusline)

# Team Claude HUD 추가
tc_status=$(~/.claude/tc-hud.sh 2>/dev/null)

# 합쳐서 출력
echo "${existing_status} │ ${tc_status}"
```

---

## 실행 절차

```
/team-claude:hud setup
        │
        ▼
┌───────────────────────────────────────────────────────────────────┐
│  STEP 1: 프리셋 선택                                              │
│                                                                   │
│  AskUserQuestion:                                                 │
│    "HUD 프리셋을 선택하세요"                                      │
│    - Full: 모든 정보                                              │
│    - Essential: 핵심만                                            │
│    - Minimal: 최소                                                │
└───────────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────────┐
│  STEP 2: 개별 항목 설정 (선택적)                                  │
│                                                                   │
│  각 항목 on/off:                                                  │
│  - 모드 표시                                                      │
│  - 진행률 바                                                      │
│  - PSM 상태                                                       │
│  - etc.                                                           │
└───────────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────────┐
│  STEP 3: 스크립트 설치                                            │
│                                                                   │
│  1. tc-hud.sh를 ~/.claude/에 복사                                │
│  2. settings.json 업데이트                                        │
│  3. 테스트 실행                                                   │
└───────────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────────┐
│  완료                                                             │
│                                                                   │
│  "HUD가 설정되었습니다."                                          │
│  "새 세션에서 statusline을 확인하세요."                           │
└───────────────────────────────────────────────────────────────────┘
```

---

## 데이터 소스

HUD는 다음 파일들에서 상태를 읽습니다:

```bash
# Flow 상태
~/.team-claude/{project-hash}/sessions/{session-id}/flow-state.json

# PSM 상태
~/.team-claude/{project-hash}/psm-index.json

# 워크플로우 상태
~/.team-claude/{project-hash}/state/workflow.json

# 리뷰 상태
~/.team-claude/{project-hash}/sessions/{session-id}/reviews/
```

---

## 캐싱

성능 최적화를 위해 HUD는 캐싱을 사용합니다:

```bash
# 캐시 파일
/tmp/tc-hud-cache-{project-hash}.json

# 캐시 TTL
300ms (0.3초)

# 캐시 무효화
- Flow 상태 변경 시
- PSM 세션 변경 시
- 파일 수정 감지 시
```

---

## 다른 Statusline과 통합

### ccstatusline과 함께 사용

```bash
#!/bin/bash
# ~/.claude/statusline.sh

# ccstatusline 출력
cc_status=$(ccstatusline "$@")

# Team Claude HUD
tc_status=$(~/.claude/tc-hud.sh 2>/dev/null)

# 결합
if [[ -n "$tc_status" ]]; then
  echo "${cc_status} │ ${tc_status}"
else
  echo "${cc_status}"
fi
```

### claude-hud와 함께 사용

```bash
#!/bin/bash
# ~/.claude/statusline.sh

# claude-hud 출력
hud_status=$(claude-hud "$@")

# Team Claude 추가
tc_status=$(~/.claude/tc-hud.sh 2>/dev/null)

if [[ -n "$tc_status" ]]; then
  echo "${hud_status} │ ${tc_status}"
else
  echo "${hud_status}"
fi
```

---

## 설정 예시

### 개발자 친화적

```yaml
hud:
  preset: essential
  display:
    mode: true
    phase: true
    progress: true
    ralph: true
    duration: true
  style:
    separator: " | "
    icons:
      autopilot: "AP"
      spec: "SP"
      impl: "IM"
```

### 이모지 중심

```yaml
hud:
  preset: full
  style:
    icons:
      autopilot: "🤖"
      assisted: "🧑‍💻"
      spec: "📝"
      impl: "⚙️"
      merge: "🔗"
      psm: "🌲"
      swarm: "🐝"
```

### 미니멀

```yaml
hud:
  preset: minimal
  display:
    mode: true
    phase: true
    progress: false
```
