---
name: team-claude:agent
description: 에이전트 관리 - 커스텀 에이전트 추가, 활성화/비활성화, 목록 조회
argument-hint: "<action> [agent-name] [options]"
allowed-tools: ["Read", "Write", "Bash", "Glob", "AskUserQuestion"]
---

# Team Claude 에이전트 관리 커맨드

계층화된 에이전트 구조를 관리합니다. 플러그인 기본 에이전트와 프로젝트별 커스텀 에이전트를 지원합니다.

## 에이전트 계층 구조

```
┌─────────────────────────────────────────────────────────────┐
│                    에이전트 해석 순서                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  1. 프로젝트 로컬 (최우선)                                   │
│     .team-claude/agents/my-custom-agent.md                 │
│                                                             │
│  2. 플러그인 기본                                            │
│     plugins/team-claude/agents/code-reviewer.md            │
│                                                             │
│  동일 이름 → 로컬이 플러그인 기본을 오버라이드               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 사용법

```bash
# 에이전트 목록 조회
/team-claude:agent list

# 에이전트 상세 보기
/team-claude:agent show <agent-name>

# 커스텀 에이전트 추가
/team-claude:agent add <agent-name>

# 에이전트 활성화/비활성화
/team-claude:agent enable <agent-name>
/team-claude:agent disable <agent-name>

# 에이전트 삭제 (커스텀만)
/team-claude:agent remove <agent-name>

# 기본 에이전트를 로컬로 복사 (커스터마이징용)
/team-claude:agent customize <agent-name>
```

---

## Action: list

모든 에이전트를 계층별로 표시합니다.

### 출력 예시

```
📋 Team Claude 에이전트 목록

━━━ 활성화됨 ━━━

  ✅ code-reviewer (기본)
     코드 품질, 컨벤션, 중복, 복잡도 검토

  ✅ qa-agent (기본)
     테스트 커버리지, 엣지 케이스, 에러 처리

  ✅ security-auditor (기본)
     인증, 권한, 입력 검증, 보안 취약점

  ✅ payment-expert (커스텀)
     결제 도메인 전문가 - 금액 계산, PG 연동
     위치: .team-claude/agents/payment-expert.md

━━━ 비활성화됨 ━━━

  ○ domain-expert (기본)
     도메인 로직 정확성, 비즈니스 규칙

  ○ performance-analyst (기본)
     성능 영향, N+1 쿼리, 메모리

활성화: /team-claude:agent enable <name>
추가:   /team-claude:agent add <name>
```

---

## Action: show

에이전트 상세 정보를 표시합니다.

### 예시

```bash
/team-claude:agent show payment-expert
```

### 출력 예시

```
📋 에이전트 상세: payment-expert

타입: 커스텀 (로컬)
상태: ✅ 활성화됨
위치: .team-claude/agents/payment-expert.md

━━━ 설정 ━━━

  model: sonnet
  tools: ["Read", "Glob", "Grep"]

━━━ 역할 ━━━

  결제 도메인 전문가
  - PG사 연동 로직 검토
  - 금액 계산 정확성 확인
  - 환불/취소 플로우 검증

━━━ 체크리스트 ━━━

  - [ ] 금액 계산 반올림 정확
  - [ ] PG 응답 에러 처리
  - [ ] 트랜잭션 롤백 처리
  - [ ] 동시성 이슈 검토
```

---

## Action: add

새 커스텀 에이전트를 생성합니다. 대화형으로 에이전트 정보를 수집합니다.

### 예시

```bash
/team-claude:agent add payment-expert
```

### 대화 흐름

```
🆕 새 에이전트 생성: payment-expert

━━━ 1/4: 기본 정보 ━━━

에이전트 설명을 입력하세요:
> 결제 도메인 전문가 - PG 연동 및 금액 계산 검토

━━━ 2/4: 모델 선택 ━━━

사용할 모델:
  1. opus (복잡한 분석, 비용 높음)
  2. sonnet (균형, 권장)
  3. haiku (빠름, 간단한 작업)

선택 [2]:

━━━ 3/4: 도구 선택 ━━━

사용할 도구 (복수 선택):
  ☑️ Read - 파일 읽기
  ☑️ Glob - 파일 검색
  ☑️ Grep - 내용 검색
  ☐ Bash - 명령 실행
  ☐ Write - 파일 작성

━━━ 4/4: 체크리스트 ━━━

리뷰 체크리스트 항목을 입력하세요 (빈 줄로 종료):
> 금액 계산 반올림 정확
> PG 응답 에러 처리
> 트랜잭션 롤백 처리
>

✅ 에이전트 생성 완료

  파일: .team-claude/agents/payment-expert.md
  상태: 활성화됨

  /team-claude:agent show payment-expert
```

### 생성되는 파일

```markdown
---
name: payment-expert
description: 결제 도메인 전문가 - PG 연동 및 금액 계산 검토
model: sonnet
tools: ["Read", "Glob", "Grep"]
---

# Payment Expert Agent

결제 도메인을 전문적으로 검토하는 에이전트입니다.

## 역할

- PG사 연동 로직 검토
- 금액 계산 정확성 확인
- 환불/취소 플로우 검증
- 결제 보안 검토

## 리뷰 체크리스트

- [ ] 금액 계산 반올림 정확
- [ ] PG 응답 에러 처리
- [ ] 트랜잭션 롤백 처리

## 리뷰 출력 형식

(기본 템플릿 사용)
```

---

## Action: enable / disable

에이전트를 활성화하거나 비활성화합니다.

### 예시

```bash
# 활성화
/team-claude:agent enable domain-expert

# 비활성화
/team-claude:agent disable security-auditor
```

### 출력 예시

```
✅ domain-expert 활성화됨

현재 활성 에이전트 (4개):
  - code-reviewer
  - qa-agent
  - security-auditor
  - domain-expert
```

### config.json 변경

```json
{
  "agents": {
    "enabled": [
      "code-reviewer",
      "qa-agent",
      "security-auditor",
      "domain-expert"
    ],
    "custom": [
      "payment-expert"
    ]
  }
}
```

---

## Action: remove

커스텀 에이전트를 삭제합니다. 기본 에이전트는 삭제할 수 없습니다.

### 예시

```bash
/team-claude:agent remove payment-expert
```

### 출력 예시

```
⚠️ 에이전트 삭제: payment-expert

이 에이전트를 삭제하시겠습니까?
  파일: .team-claude/agents/payment-expert.md

  [Y/n]

✅ payment-expert 삭제됨
```

### 기본 에이전트 삭제 시도

```
❌ 기본 에이전트는 삭제할 수 없습니다: code-reviewer

비활성화: /team-claude:agent disable code-reviewer
커스터마이징: /team-claude:agent customize code-reviewer
```

---

## Action: customize

기본 에이전트를 로컬로 복사하여 커스터마이징합니다.

### 예시

```bash
/team-claude:agent customize code-reviewer
```

### 출력 예시

```
📝 에이전트 커스터마이징: code-reviewer

기본 에이전트를 로컬로 복사합니다.
복사 후 로컬 버전이 우선 적용됩니다.

복사됨:
  원본: plugins/team-claude/agents/code-reviewer.md
  대상: .team-claude/agents/code-reviewer.md

✅ 커스터마이징 준비 완료

수정: .team-claude/agents/code-reviewer.md
원복: /team-claude:agent remove code-reviewer
```

---

## config.json 에이전트 스키마

```json
{
  "agents": {
    "enabled": [
      "code-reviewer",
      "qa-agent",
      "security-auditor",
      "payment-expert"
    ],
    "custom": [
      "payment-expert",
      "fraud-detector"
    ],
    "overrides": {
      "code-reviewer": {
        "model": "opus"
      }
    }
  }
}
```

### 필드 설명

| 필드 | 설명 |
|------|------|
| `enabled` | 활성화된 에이전트 목록 (기본 + 커스텀) |
| `custom` | 커스텀 에이전트 목록 (로컬에 정의된 것) |
| `overrides` | 기본 에이전트 설정 오버라이드 |

---

## 에이전트 해석 로직

```
1. config.json의 enabled 목록 확인

2. 각 에이전트에 대해:
   a. .team-claude/agents/{name}.md 존재? → 사용
   b. 없으면 plugins/team-claude/agents/{name}.md 사용

3. overrides 적용 (있으면)
```

### 예시: code-reviewer 해석

```
enabled에 "code-reviewer" 있음
  ↓
.team-claude/agents/code-reviewer.md 확인
  ↓
존재함 → 로컬 버전 사용 (커스터마이징된 버전)
  ↓
overrides["code-reviewer"] 확인
  ↓
{ "model": "opus" } → 모델을 opus로 오버라이드
```

---

## 기본 에이전트 목록

| 이름 | 설명 | 기본 활성화 |
|------|------|------------|
| code-reviewer | 코드 품질, 컨벤션 | ✅ |
| qa-agent | 테스트, 엣지 케이스 | ✅ |
| security-auditor | 보안 취약점 | ✅ |
| domain-expert | 도메인 로직 | ○ |
| performance-analyst | 성능 분석 | ○ |
| architecture-reviewer | 아키텍처 검토 | ○ |

---

## 에러 처리

### 에이전트 없음

```
❌ 에이전트를 찾을 수 없습니다: unknown-agent

사용 가능한 에이전트:
  기본: code-reviewer, qa-agent, security-auditor, ...
  커스텀: payment-expert
```

### 이름 충돌

```
⚠️ 이미 존재하는 에이전트입니다: code-reviewer

기본 에이전트를 커스터마이징하려면:
  /team-claude:agent customize code-reviewer
```

### 활성화 중복

```
ℹ️ 이미 활성화되어 있습니다: qa-agent
```
