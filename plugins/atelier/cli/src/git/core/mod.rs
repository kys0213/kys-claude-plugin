//! Core services for `atelier git` (ported from git-utils `src/core/`).
//! Shell-out helpers and the injectable service traits live here.

pub mod git;
pub mod github;
pub mod jira;
pub mod pr_guard;
pub mod shell;
