{
  "name": "@git-utils/cli",
  "version": "3.0.0-alpha.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/cli.ts",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "build": "bun build --compile src/cli.ts --outfile dist/git-utils",
    "install-cli": "bun run src/installer.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
